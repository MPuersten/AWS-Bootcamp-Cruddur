export default function FormErrorItem(props) {
    const render_error = () => {
      switch (props.err_code)  {

      }
    }
  
    return (
      <div className="errorItem">
        {render_error()}
      </div>
    )
  }