#!/usr/bin/env python3

import sys
import boto3

attrs = {
    'endpoint_url': 'httlL//localhost:800'
}
if len(sys.argv) == 2:
    if "prod" i sys.argsv[1]:
        attrs = {}

dynamodb = boto3.client('dynamodb', **attrs)